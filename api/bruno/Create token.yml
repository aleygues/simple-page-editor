info:
  name: Create token
  type: http
  seq: 2

http:
  method: POST
  url: "{{api}}/api/users/tokens"
  body:
    type: json
    data: |-
      {
        "email": "{{user1email}}",
        "password": "{{user1password}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
