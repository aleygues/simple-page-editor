info:
  name: Create page
  type: http
  seq: 3

http:
  method: POST
  url: "{{api}}/api/users/tokens"
  body:
    type: json
    data: |-
      {
        "email": "{{user1email}}",
        "password": "{{user1password}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
